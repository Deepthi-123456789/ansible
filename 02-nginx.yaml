- name: install and run nginx
  host: localhost
  become: yes
  task:
  - name: install nginx
    ansible.builtin.package:
    - name: nginx
      state: present
  - name: run nginx
    ansible.builtin.service:
    - name: nginx run
      state: starting

