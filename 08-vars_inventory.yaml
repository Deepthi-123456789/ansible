- name: variables in inventory
  hosts: localhost
  tasks:
  - name: invetory
    ansible.builtin.debug:
      msg: "hello iam learning {{course}} and trainer is {{trainer}} and duriation is {{duriation}}"